PORT=5000

# Local MySQL / XAMPP
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=agrabo_db

# Railway MySQL usually provides MYSQL_URL. Use that in production.
# MYSQL_URL=mysql://USER:PASSWORD@HOST:PORT/DATABASE

JWT_SECRET=replace_with_a_long_random_secret
WHATSAPP_NUMBER=256706506319

# Comma-separated frontend URLs allowed to call the API.
CLIENT_URLS=http://localhost:4200,http://127.0.0.1:4201
